/*
* "dc_Common" libraries are somewhat different than
* other "dc_" libraries in that they contain code that
* is tailored to the specific module. 
* 
* This gives us a central (common) location for code
* that is reused frequently by different models, levels
* and so on, but that we still need to customize for
* different module needs.
*/

#include "data/scripts/dc_common_onmovea/config.h"

#import "data/scripts/dc_common_onmovea/dc_common_onmovea.c"